# Examples

`sample_input.md` intentionally contains formulaic writing so `article_lint.py` has something obvious to flag.

`zhuque-response.example.json` is synthetic and is used only to demonstrate parsing/gating. It is not a real detector result.
